# OpenAI API key — used for embeddings (text-embedding-3-small) and chat completions (gpt-4o-mini)
OPENAI_API_KEY=

# Server settings (optional, used if you wire them into main.py / uvicorn)
HOST=0.0.0.0
PORT=8000
